# Blog Post

## 1. What are some differences between interfaces and types in TypeScript?

TypeScript-এ `interface` এবং `type` উভয়ই ডেটার কাঠামো (structure) সংজ্ঞায়িত করতে ব্যবহৃত হয়, তবে ক্ষমতা এবং ব্যবহারের দিক থেকে তাদের মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য রয়েছে। 

### মূল পার্থক্যসমূহ

| বৈশিষ্ট্য | `interface` | `type` (Type Alias) |
| :--- | :--- | :--- |
| **ডেটার ধরন** | মূলত **অবজেক্টের** কাঠামো বর্ণনা করতে ব্যবহৃত হয়। | **অবজেক্ট**, **ইউনিয়ন**, **ইন্টারসেকশন**, **প্রিমিটিভ** এবং **টুপল** বর্ণনা করতে পারে। |
| **এক্সটেনশন** | `extends` কিওয়ার্ড ব্যবহার করে **এক্সটেন্ড** করা যায়। | সরাসরি এক্সটেন্ড করা যায় না, তবে **ইন্টারসেকশন** (`&`) ব্যবহার করে **কম্বাইন** করা যায়। |
| **ডিক্লারেশন মার্জিং** | **ডিক্লারেশন মার্জিং সাপোর্ট করে** (একাধিকবার সংজ্ঞায়িত করা হলে প্রপার্টিগুলি একত্রিত হয়)। | **ডিক্লারেশন মার্জিং সাপোর্ট করে না**। |
| **ইমপ্লিমেন্টেশন** | ক্লাস দ্বারা `implements` কিওয়ার্ড ব্যবহার করে **ইমপ্লিমেন্ট** করা যায়। | ক্লাস দ্বারা সরাসরি ইমপ্লিমেন্ট করা যায় না। |

### 💡 উদাহরণ

```typescript
// Interface
interface Person {
  name: string;
  age: number;
}

// Interface Extension
interface Employee extends Person {
  employeeId: number;
}

// Type
type ID = string | number; // Union Type
type User = Person & { userId: ID }; // Intersection Type
````

-----

## 2. Explain the difference between any, unknown, and never types in TypeScript.

এই তিনটি বিশেষ Type TypeScript-এ Type Safety এবং Control Flow পরিচালনার ক্ষেত্রে গুরুত্বপূর্ণ ভূমিকা রাখে।

### `any` (Escape Hatch)

`any` Type আপনাকে TypeScript-এর Type Checking পুরোপুরি এড়িয়ে যেতে দেয়। আপনি কম্পাইলারের কোনো ত্রুটি ছাড়াই `any` ভেরিয়েবলের উপর **যেকোনো অপারেশন** করতে পারেন। এটি Type Safety নষ্ট করে, তাই এটি সাবধানে ব্যবহার করা উচিত।

```typescript
let anyValue: any = "hello";
anyValue.toUpperCase(); // কাজ করবে
anyValue.nonExistentMethod(); // কম্পাইল হবে, কিন্তু রানটাইমে ত্রুটি দেবে।
```

### `unknown` (Type-Safe `any`)

`unknown` Type এমন একটি মানকে উপস্থাপন করে, যার Type ঘোষণার সময় **জানা যায় না**। `any`-এর বিপরীতে, আপনি কোনো অপারেশন করার আগে অবশ্যই Type Narrowing (যেমন: `typeof`, `instanceof` ব্যবহার করে) করতে বাধ্য। এটি Type Safety নিশ্চিত করে এবং `any`-এর চেয়ে বেশি পছন্দনীয়।

```typescript
let unknownValue: unknown = "hello";
// unknownValue.toUpperCase(); // এরর: Object is of type 'unknown'

if (typeof unknownValue === "string") {
  unknownValue.toUpperCase(); // Type চেক করার পর কাজ করবে
}
```

### `never` (অসম্ভব Type)

`never` Type এমন মানের Type উপস্থাপন করে যা **কখনোই ঘটে না**। এটি সাধারণত এমন ফাংশনগুলির রিটার্ন Type হিসাবে ব্যবহৃত হয় যা:

1.  **একটি ত্রুটি নিক্ষেপ করে** (এবং স্বাভাবিকভাবে কখনই ফেরে না)।
2.  **একটি অনন্ত লুপ ধারণ করে**।
3.  **Exhaustive Checking** (ইউনিয়ন টাইপের সমস্ত কেস পরিচালনা নিশ্চিত করা) এর জন্য ব্যবহৃত হয়।

<!-- end list -->

```typescript
function throwError(message: string): never {
  throw new Error(message);
}

function infiniteLoop(): never {
  while (true) {}
}